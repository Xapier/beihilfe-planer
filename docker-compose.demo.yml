version: '3.8'

services:
  beihilfe-backend-demo:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - NODE_ENV=development
      - DB_PATH=/app/data/beihilfe.db

  beihilfe-frontend-demo:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./app:/usr/share/nginx/html
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro


